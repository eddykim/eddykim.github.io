# The Site Configuration

# Import the theme
theme: jekyll-theme-chirpy

# The language of the webpage › http://www.lingoes.net/en/translator/langcode.htm
# If it has the same name as one of the files in folder `_data/locales`, the layout language will also be changed,
# otherwise, the layout language will use the default value of 'en'.
lang: ko-KR

# Change to your timezone › https://zones.arilyn.cc
timezone: Asia/Seoul

# jekyll-seo-tag settings › https://github.com/jekyll/jekyll-seo-tag/blob/master/docs/usage.md
# ↓ --------------------------

title: Ed Kim — 광학 계측 연구노트 # the main title

tagline: 박막 계측 · 컴퓨테이셔널 옵틱스 · 엘립소메트리 # it will display as the subtitle

description: >- # used by seo meta and the atom feed
  광학 계측(박막 두께 측정, 엘립소메트리, 컴퓨테이셔널 옵틱스)을 연구하고 만드는 과정을 기록하는 블로그.

# Fill in the protocol & hostname for your site.
# E.g. 'https://username.github.io', note that it does not end with a '/'.
url: "https://eddykim.github.io"

github:
  username: eddykim # change to your GitHub username

twitter:
  username: # change to your Twitter username

social:
  # Change to your full name.
  # It will be displayed as the default author of the posts and the copyright owner in the Footer
  name: Youngjoon (Ed) Kim
  email: eddyoptics@gmail.com # change to your email address
  fediverse_handle: # fill in your fediverse handle. E.g. "@username@domain.com"
  links:
    # The first element serves as the copyright owner's link
    - https://github.com/eddykim # change to your Twitter homepage
    - https://www.linkedin.com/in/youngjoon-kim-ed/
    # Uncomment below to add more social links
    # - https://www.facebook.com/username

# Site Verification Settings
webmaster_verifications:
  google: # fill in your Google verification code
  bing: # fill in your Bing verification code
  alexa: # fill in your Alexa verification code
  yandex: # fill in your Yandex verification code
  baidu: # fill in your Baidu verification code
  facebook: # fill in your Facebook verification code

# ↑ --------------------------
# The end of `jekyll-seo-tag` settings

# Web Analytics Settings
analytics:
  google:
    # GA4 measurement ID, e.g. "G-XXXXXXXXXX"
    # › https://analytics.google.com  →  관리 > 데이터 스트림 > 웹 스트림 추가
    id: G-CL3K08SH7Q
  goatcounter:
    # GoatCounter site code only (the "xxx" in https://xxx.goatcounter.com), NOT the full URL.
    # › https://www.goatcounter.com/signup  (비상업 사이트 무료)
    # 이 값이 비어 있으면 아래 pageviews 카운터는 화면에 표시되지 않습니다.
    id: eddykim
  umami:
    id: # fill in your Umami ID
    domain: # fill in your Umami domain
  matomo:
    id: # fill in your Matomo ID
    domain: # fill in your Matomo domain
  cloudflare:
    id: # fill in your Cloudflare Web Analytics token
  fathom:
    id: # fill in your Fathom Site ID

# Page views settings
# 글 상단/목록의 조회수 표시. `analytics.goatcounter.id`가 채워져야 실제로 렌더링됩니다.
pageviews:
  provider: goatcounter # now only supports 'goatcounter'

# Prefer color scheme setting.
#
# Note: Keep empty will follow the system prefer color by default,
# and there will be a toggle to switch the theme between dark and light
# on the bottom left of the sidebar.
#
# Available options:
#
#     light — Use the light color scheme
#     dark — Use the dark color scheme
#
theme_mode: # [light | dark]

# The CDN endpoint for media resources.
# Notice that once it is assigned, the CDN url
# will be added to all media resources (site avatar, posts' images, audio and video files) paths starting with '/'
#
# e.g. 'https://cdn.com'
cdn:

# the avatar on sidebar, support local or CORS resources
avatar: /assets/img/avatar.jpg

# The URL of the site-wide social preview image used in SEO `og:image` meta tag.
# It can be overridden by a customized `page.image` in front matter.
social_preview_image: # string, local or CORS resources

# boolean type, the global switch for TOC in posts.
toc: true

actions:
  # Display "Edit this post" action on each post page to encourage contributions.
  edit_post:
    enabled: false # set to true to display the "Edit this post" action.
    url: "" # repository base URL, e.g. https://github.com/cotes2020/jekyll-theme-chirpy/edit/master
    # Use dynamic or static URL for the edit link.
    # If `static_url` is false, the post's path will be appended to the `url` above as the final edit link.
    # Set to true if the URL should remain unchanged, which means each post will share the same edit link.
    static_url: false # [ true | false ]

# Google AdSense — 기본 꺼짐.
# 승인 후 enabled: true + publisher_id 를 채우면 _includes/metadata-hook.html 이
# 프로덕션 빌드에서만 광고 스크립트를 삽입합니다. ads.txt.example 도 함께 참고하세요.
adsense:
  enabled: false
  publisher_id: # "ca-pub-0000000000000000"

comments:
  # Global switch for the post-comment system. Keeping it empty means disabled.
  provider: giscus # [disqus | utterances | giscus]
  # The provider options are as follows:
  disqus:
    shortname: # fill with the Disqus shortname. › https://help.disqus.com/en/articles/1717111-what-s-a-shortname
  # utterances settings › https://utteranc.es/
  utterances:
    repo: # <gh-username>/<repo>
    issue_term: # < url | pathname | title | ...>
  # Giscus options › https://giscus.app
  giscus:
    repo: eddykim/eddykim.github.io
    repo_id: R_kgDOTwrwTg
    category: Comments # Discussions에 만들 카테고리 이름 (Announcements 형식 권장)
    category_id: DIC_kwDOTwrwTs4DE3Wb
    mapping: pathname
    strict: "1"
    input_position: bottom
    lang: ko
    reactions_enabled: "1"

# Self-hosted static assets, optional › https://github.com/cotes2020/chirpy-static-assets
assets:
  self_host:
    enabled: # boolean, keep empty means false
    # specify the Jekyll environment, empty means both
    # only works if `assets.self_host.enabled` is 'true'
    env: # [development | production]

pwa:
  enabled: true # The option for PWA feature (installable)
  cache:
    enabled: true # The option for PWA offline cache
    # Paths defined here will be excluded from the PWA cache.
    # Usually its value is the `baseurl` of another website that
    # shares the same domain name as the current website.
    deny_paths:
      # - "/example"  # URLs match `<SITE_URL>/example/*` will not be cached by the PWA

# 미래 날짜로 적은 글도 즉시 발행한다.
# GitHub Pages는 푸시가 있을 때만 빌드하므로 future: false(기본값)이면
# 예약 시각이 지나도 자동으로 올라오지 않고 다음 푸시까지 404가 된다.
# 글 노출 순서는 그대로 date 기준을 따른다.
future: true

paginate: 10

# The base URL of your site
baseurl: ""

# ------------ The following options are not recommended to be modified ------------------

kramdown:
  footnote_backlink: "&#8617;&#xfe0e;"
  syntax_highlighter: rouge
  syntax_highlighter_opts: # Rouge Options › https://github.com/jneen/rouge#full-options
    css_class: highlight
    # default_lang: console
    span:
      line_numbers: false
    block:
      line_numbers: true
      start_line: 1

collections:
  tabs:
    output: true
    sort_by: order

defaults:
  - scope:
      path: "" # An empty string here means all files in the project
      type: posts
    values:
      layout: post
      comments: true # Enable comments in posts.
      toc: true # Display TOC column in posts.
      # DO NOT modify the following parameter unless you are confident enough
      # to update the code of all other post links in this project.
      permalink: /posts/:title/
  - scope:
      path: _drafts
    values:
      comments: false
  - scope:
      path: ""
      type: tabs # see `site.collections`
    values:
      layout: page
      permalink: /:title/

sass:
  style: compressed

compress_html:
  clippings: all
  comments: all
  endings: all
  profile: false
  blanklines: false
  ignore:
    envs: [development]

exclude:
  - "*.gem"
  - ads.txt.example
  - "*.gemspec"
  - docs
  - tools
  - README.md
  - LICENSE
  - purgecss.js
  - "*.config.js"
  - "package*.json"

jekyll-archives:
  enabled: [categories, tags]
  layouts:
    category: category
    tag: tag
  permalinks:
    tag: /tags/:name/
    category: /categories/:name/
